hl.config({
    group = {
        groupbar = {
            indicator_gap = -6,
            indicator_height = 3,
            rounding = 0,
            render_titles = false,
            round_only_edges = true,
        }
    }
})

require("hyprland.core.config.group.groupbar.col")

